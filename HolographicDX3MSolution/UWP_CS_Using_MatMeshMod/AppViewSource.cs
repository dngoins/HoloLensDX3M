using Windows.ApplicationModel.Core;

namespace UWP_CS_Using_MatMeshMod
{
    // The entry point for the app.
    internal class AppViewSource : IFrameworkViewSource
    {
        public IFrameworkView CreateView()
        {
            return new AppView();
        }
    }
}
